# 금융상품 Agent — 단계별 기술 선택지 및 장단점

> 미래에셋증권 AI Festival 2026
> 각 단계에서 선택 가능한 기술 후보, 장단점, 정확도 비교 정리

---

## 전체 파이프라인

```
[1] 데이터 전처리·정제
[2] 온톨로지 설계 (TTL 필수 제출)
[3] RDB 구축
[4] Vector DB 구축 (임베딩 모델 선택 포함)
[5] Graph DB 구축
[6] 검색 전략 (NL2SQL / Hybrid Search / Re-ranker)
[7] Intent 분류 & Agent 오케스트레이션
[8] 답변 생성 & 환각 방지
[9] API 서버
```

---

## 단계 1. 데이터 전처리

### 1-A. 정형 데이터 처리 라이브러리

| 후보 | 장점 | 단점 | 정확도 영향 |
|------|------|------|------------|
| **pandas** | 범용성, 생태계 풍부, 결측값 처리 함수 다양 | 대용량 메모리 사용 | 중립 |
| polars | pandas보다 10~100배 빠름, 메모리 효율 | 생태계 상대적으로 작음 | 중립 |
| DuckDB | SQL로 xlsx 직접 쿼리 가능, 집계 특화 | 전처리 유연성 낮음 | 중립 |

> **권장**: pandas. 14.5만 건은 pandas로 충분하며, 전처리 유연성이 가장 높다.

### 1-B. 결측값 처리 전략

| 전략 | 설명 | 정확도 영향 |
|------|------|------------|
| **명시적 NULL 유지** | 결측을 NULL로 유지, SQL에서 IS NULL 처리 | 높음 — 없는 정보를 없다고 정확히 표현 |
| 기본값 대체 | 결측을 0이나 "미공개"로 채움 | 낮음 — 잘못된 필터 결과 유발 가능 |
| 행 제거 | 결측 행 삭제 | 낮음 — 실제 상품 누락 |

> **권장**: 명시적 NULL 유지. "총보수 정보 없음"을 "0%"로 잘못 해석하면 치명적 오류.

### 1-C. 비정형 데이터 파싱 (투자설명서 PDF)

| 후보 | 장점 | 단점 | 텍스트 품질 |
|------|------|------|------------|
| **pymupdf (fitz)** | 빠름, 레이아웃 보존, 표 추출 지원 | 복잡한 PDF에서 줄바꿈 오류 | 상 |
| pdfminer.six | 정밀한 텍스트 위치 추출 | 느림, 코드 복잡 | 중상 |
| markitdown | Markdown 변환 자동화, LLM 친화적 | Microsoft 의존성 | 중 |
| pdfplumber | 표 추출 특화 | 일반 텍스트 추출 느림 | 중 |

> **권장**: pymupdf. 속도와 품질 균형이 가장 좋다.

### 1-D. Entity Resolution (운용사명 통일)

| 전략 | 설명 | 정확도 영향 |
|------|------|------------|
| **하드코딩 사전** | "삼성운용" → "삼성자산운용" 매핑 테이블 | 높음 — 확실한 매핑 |
| fuzzy matching | rapidfuzz로 유사도 기반 자동 매핑 | 중간 — 오매핑 위험 |
| LLM 정규화 | LLM에게 운용사명 통일 요청 | 낮음 — 비용·속도 과다 |

> **권장**: 하드코딩 사전 + fuzzy matching 보조. 사전에 없는 경우 fuzzy로 후보 제시 후 수동 검토.

---

## 단계 2. 온톨로지 설계

### 2-A. 설계 방법

| 후보 | 장점 | 단점 | 품질 |
|------|------|------|------|
| **수동 설계 (Protégé)** | 도메인 의미 정확, 관계 세밀 제어 | 시간 소요 | 최상 |
| LLM 보조 설계 | 빠른 초안 생성 | 검토·수정 필수, 오류 포함 가능 | 중 |
| 스키마 자동 매핑 | 컬럼명 → OWL Property 자동 변환 | 도메인 의미 손실 | 하 |

> **권장**: LLM으로 초안 생성 → Protégé로 검토·수정. 시간 절약과 품질 확보 균형.

### 2-B. 직렬화 형식

| 형식 | 장점 | 단점 |
|------|------|------|
| **Turtle (.ttl)** | 가독성 높음, 평가 제출 요구 형식 | — |
| OWL/XML | 도구 호환성 높음 | 가독성 낮음 |
| JSON-LD | 웹 친화적 | 온톨로지 추론 도구 호환 낮음 |

> **권장**: Turtle. 제출 요건이 .ttl이므로 선택의 여지 없음.

### 2-C. 온톨로지 라이브러리

| 후보 | 장점 | 단점 | 정확도 영향 |
|------|------|------|------------|
| **rdflib** | Python 네이티브, TTL 파싱·생성·SPARQL | 추론 기능 제한 | 중립 |
| owlready2 | Python 객체로 OWL 매핑, 추론 내장 | rdflib보다 느림 | 중립 |
| Apache Jena (Java) | 강력한 SPARQL 엔진, 대규모 RDF | Java 환경 필요 | 중립 |

> **권장**: rdflib (TTL 생성·검증) + Apache Jena (Graph DB 적재·SPARQL).

---

## 단계 3. RDB 구축

### 3-A. RDBMS 선택

| 후보 | 장점 | 단점 | 정확도 영향 |
|------|------|------|------------|
| **PostgreSQL** | 안정성·성능 우수, JSON 지원, Full-text search | 설정 필요 | 높음 |
| SQLite | 설정 없음, 파일 기반 | 동시 쓰기 제한, 대용량 느림 | 중간 |
| DuckDB | 분석 쿼리 특화 (집계·정렬 빠름), pandas 연동 | OLTP 약함 | 높음 (집계 질의) |
| MySQL | 범용, NCP 지원 | JSON·Full-text 기능 PostgreSQL 대비 약함 | 중간 |

> **정확도 측면**: DuckDB는 "수익률 상위 N개", "평균 보수 계산" 등 집계 쿼리에서 PostgreSQL보다 빠르고 정확한 결과.
> **권장**: PostgreSQL (운영 환경 안정성) + DuckDB (복잡한 집계 쿼리 병행).

### 3-B. 인덱스 전략

| 전략 | 설명 | 정확도 영향 |
|------|------|------------|
| **필터 컬럼 인덱스** | 위험등급·자산군·지역·만기일에 B-tree 인덱스 | 쿼리 속도 향상, 60초 제한 준수에 유리 |
| 복합 인덱스 | (자산군, 지역) 복합 인덱스 | 자주 함께 쓰이는 조건 조합에 효과적 |
| Full-text 인덱스 | 상품명 LIKE 검색 가속 | 상품명 키워드 검색 정확도 향상 |

---

## 단계 4. Vector DB 구축

### 4-A. Vector DB 엔진 선택

| 후보 | 장점 | 단점 | 검색 정확도 |
|------|------|------|------------|
| **Chroma** | Python 네이티브, 설정 간단, 메타데이터 필터 지원 | 대규모(수십만) 성능 미검증 | 중상 |
| **FAISS** | 매우 빠름, 메모리 효율, 오픈소스 | 메타데이터 필터 기본 미지원, 직접 구현 필요 | 중상 |
| Qdrant | 메타데이터 필터 강력, REST API, 정확도 높음 | 별도 서버 필요 | 상 |
| Milvus | 대규모(수백만) 특화, GPU 지원 | 설정 복잡, 오버스펙 | 상 |
| Weaviate | 하이브리드 검색 내장 (BM25+Vector) | 설정 복잡 | 상 |

> **정확도 측면**: Qdrant > Weaviate > Chroma ≈ FAISS (메타데이터 필터 결합 시 Qdrant가 유리)
> **권장**: Chroma (빠른 개발) 또는 Qdrant (메타데이터 필터 정확도 중요 시).
> 14.5만 건 + 구성종목 청크까지 합치면 약 20~30만 벡터 → Chroma·FAISS 모두 충분.

### 4-B. 임베딩 모델 선택

| 후보 | 장점 | 단점 | 한국어 정확도 | 영어 정확도 |
|------|------|------|-------------|------------|
| **HyperCLOVA X Embedding** | LLM과 같은 생태계, 한국어 최적화, NCP 통합 | API 비용, 레이턴시 | 최상 | 중 |
| multilingual-e5-large | 한영 혼재 텍스트 강함, 로컬 실행 | 모델 크기 큼 (560MB) | 상 | 상 |
| BGE-M3 | 다국어 최강, BM25+Dense 동시 지원 | 모델 크기 큼 | 상 | 상 |
| KoSimCSE-roberta | 한국어 문장 유사도 특화, 경량 | 영문 약함 | 상 | 하 |
| text-embedding-3-small | 빠름, 저렴 | OpenAI 의존 (규정 위반 가능성) | 중 | 상 |

> **정확도 측면**:
> - 한국어 상품명·운용전략 → HyperCLOVA X Embedding 최우선
> - 해외ETF 영문 운용전략 → multilingual-e5 또는 BGE-M3가 유리
> - **권장**: HyperCLOVA X Embedding (한국어 일관성) + multilingual-e5 (영문 청크 전용)

### 4-C. 청크 크기 전략

| 전략 | 설명 | 정확도 영향 |
|------|------|------------|
| **고정 크기 청크** | 토큰 수 기준으로 자름 (예: 256 토큰) | 문장 중간에 잘릴 수 있어 의미 손실 |
| **문장 단위 청크** | 문장 경계 기준으로 분리 | 의미 보존, 상품 요약에 적합 |
| **섹션 단위 청크** | 투자설명서의 "운용전략", "위험요인" 섹션별 분리 | 높음 — 질의 의도와 청크 내용 일치 |
| **슬라이딩 윈도우** | 겹치는 구간 포함 (overlap) | 경계 정보 손실 방지, 재현율 향상 |

> **권장**: 상품 요약은 문장 단위, 투자설명서는 섹션 단위 + 슬라이딩 윈도우.

### 4-D. 유사도 측정 방식

| 후보 | 설명 | 정확도 |
|------|------|--------|
| **Cosine Similarity** | 벡터 방향 유사도, 길이 무관 | 상 (표준) |
| Dot Product | 빠름, 정규화된 벡터에서 Cosine과 동일 | 상 |
| Euclidean Distance | 절대 거리 기반 | 중 (임베딩 공간에서 비직관적) |

> **권장**: Cosine Similarity (임베딩 모델 표준, 길이 정규화 자동 적용).

---

## 단계 5. Graph DB 구축

### 5-A. Graph DB 엔진 선택

| 후보 | 쿼리 언어 | 장점 | 단점 | 정확도 영향 |
|------|----------|------|------|------------|
| **Neo4j Community** | Cypher | 가장 범용, 시각화 내장, Python 드라이버 | Community 버전 제약 | 상 |
| Apache Jena + TDB2 | SPARQL | TTL 파일 직접 로드, OWL 추론 내장 | Java 환경, 학습 곡선 | 최상 (온톨로지 연동) |
| Stardog | SPARQL | 엔터프라이즈 추론, OWL 완전 지원 | 비용 | 최상 |
| NetworkX | Python API | 설치 없음, 경량 | 대규모 탐색 느림, 영속성 없음 | 중 |
| Amazon Neptune | SPARQL/Gremlin | 클라우드 관리형 | NCP 환경과 맞지 않음 | 상 |

> **정확도 측면**:
> - 온톨로지 추론 (SUBSIDIARY_OF 관계 자동 추론 등) → Apache Jena가 최상
> - 일반 관계 탐색 → Neo4j가 개발 속도·편의성 우수
> **권장**: Neo4j (관계 탐색 주력) + rdflib (TTL SPARQL 보조).

### 5-B. 그래프 모델 전략

| 전략 | 설명 | 정확도 영향 |
|------|------|------------|
| **Property Graph** | 노드·엣지에 속성 부여 (Neo4j Cypher) | 높음 — 속성 기반 필터 가능 |
| RDF Triple Store | 주어-술어-목적어 트리플 (SPARQL) | 높음 — 온톨로지 추론과 직접 연동 |
| 혼합 | Neo4j로 관계 탐색 + rdflib로 TTL 관리 | 최상 — 두 방식의 장점 결합 |

---

## 단계 6. 검색 전략

### 6-A. NL2SQL 방법론

| 후보 | 설명 | 장점 | 단점 | 정확도 |
|------|------|------|------|--------|
| **Few-shot Prompting** | 스키마 + 예시 SQL 수십 개 주입 | 구현 단순 | 예시 없는 패턴에서 오류 | 중 |
| **Schema Linking + CoT** | 컬럼 매핑 단계 분리 후 단계적 SQL 생성 | 복잡한 조건 처리 강함 | LLM 호출 2회 | 상 |
| DAIL-SQL 방식 | 질의와 유사한 예시 SQL 동적 선택 후 주입 | 높은 정확도 | 유사 예시 DB 구축 필요 | 최상 |
| Fine-tuning | NL2SQL 전용 모델 학습 | 최고 정확도 (학습 데이터 충분 시) | 학습 데이터 부족, HyperCLOVA X fine-tune 어려움 | — |

> **정확도 측면**: DAIL-SQL > Schema Linking + CoT > Few-shot
> **권장**: Schema Linking + CoT (구현 가능성과 정확도 균형). 예시 SQL을 30~50개 준비하면 정확도 대폭 향상.

### 6-B. 벡터 검색 전략

| 후보 | 설명 | 장점 | 단점 | 정확도 |
|------|------|------|------|--------|
| Dense-only | 임베딩 벡터 유사도만 사용 | 의미 검색 강함 | 정확한 키워드 매칭 약함 | 중상 |
| Sparse-only (BM25) | 키워드 빈도 기반 | 정확한 단어 매칭 강함 | 의미 검색 불가 | 중 |
| **Hybrid (BM25 + Dense)** | 두 방식 결합 | 키워드·의미 모두 커버 | 구현 복잡도 증가 | 최상 |

> **정확도 측면**: Hybrid가 단독 방식 대비 Recall 20~30% 향상 (논문 기준).
> **권장**: Hybrid. "에코프로비엠"(정확 키워드) + "2차전지 소재 기업"(의미) 동시 대응.

### 6-C. 결과 결합 방식 (Hybrid Search)

| 후보 | 설명 | 정확도 |
|------|------|--------|
| **RRF (Reciprocal Rank Fusion)** | 순위 기반 결합, 파라미터 없음 | 상 (안정적) |
| 가중 합산 | BM25 점수 × α + Dense 점수 × β | 상 (튜닝 시) |
| Borda Count | 순위 투표 방식 | 중 |

> **권장**: RRF. 파라미터 튜닝 없이 안정적인 성능.

### 6-D. Re-ranker (선택)

| 후보 | 설명 | 장점 | 단점 | 정확도 향상 |
|------|------|------|------|------------|
| **Cross-encoder** | 쿼리-문서 쌍을 함께 인코딩 | 높은 정확도 | 느림 (문서 수 × 인코딩) | +10~20% |
| LLM Re-ranking | HyperCLOVA X에 관련도 판단 요청 | 도메인 이해 높음 | LLM 호출 비용 | +15~25% |
| ColBERT | Token-level 상호작용 | 빠른 Cross-encoder | 모델 크기 큼 | +10~15% |

> **정확도 측면**: LLM Re-ranking > Cross-encoder > ColBERT
> **권장**: 60초 여유가 있으므로 LLM Re-ranking 적용. 상위 20개 후보를 HyperCLOVA X로 재순위.

---

## 단계 7. Intent 분류 & Agent 오케스트레이션

### 7-A. Intent 분류 방식

| 후보 | 설명 | 장점 | 단점 | 정확도 |
|------|------|------|------|--------|
| **규칙 기반** | 키워드 패턴 매칭 (regex) | 빠름, 예측 가능 | 복잡한 질의 처리 불가 | 하 |
| **LLM 분류 (Query Plan)** | HyperCLOVA X가 JSON 형식으로 분류·분해 | 복잡한 질의 처리, 엔티티 추출 동시 | LLM 호출 1~2회 추가 | 최상 |
| Fine-tuned Classifier | 분류 전용 소형 모델 | 빠름, 비용 낮음 | 학습 데이터 필요, HyperCLOVA X 생태계 이탈 | 상 |

> **정확도 측면**: LLM 분류가 압도적으로 높음. 복합 질의에서 규칙 기반은 한계.
> **권장**: 규칙 기반(답변 불가 사전 감지) + LLM 분류(나머지 모든 질의).

### 7-B. Agent 프레임워크

| 후보 | 설명 | 장점 | 단점 | 정확도 |
|------|------|------|------|--------|
| **LangGraph** | 상태 기반 DAG, 루프·분기 명시적 제어 | 복잡한 흐름 정확히 제어, 디버깅 용이 | 학습 곡선 | 상 |
| LangChain | 범용 Agent 프레임워크, HyperCLOVA X 연동 | 빠른 개발, 풍부한 예제 | 복잡한 흐름 제어 어려움 | 중상 |
| HyperCLOVA X Function Calling | NCP Clova Studio 네이티브 Tool Use | NCP 생태계 통일, 레이턴시 낮음 | 프레임워크 기능 제한 | 상 |
| 자체 구현 (Python) | 최대 유연성, 의존성 없음 | 원하는 대로 제어 | 개발 시간 많이 소요 | 최상 (잘 구현 시) |

> **정확도 측면**: 자체 구현 > LangGraph > HyperCLOVA X Function Calling > LangChain
> **권장**: LangGraph. 복잡한 Federated Query 분기를 명시적으로 제어 가능.

### 7-C. Agent 패턴

| 패턴 | 설명 | 장점 | 단점 | 정확도 |
|------|------|------|------|--------|
| **ReAct** | Thought → Action → Observation 반복 | 단계적 추론, 오류 수정 가능 | LLM 호출 多 | 상 |
| **Plan-and-Execute** | 전체 계획 먼저 수립 → 순서대로 실행 | 일관된 흐름, 예측 가능 | 계획 오류 시 전체 실패 | 상 |
| **Query Plan JSON** | LLM이 실행 계획을 JSON으로 출력 → 코드가 실행 | 계획·실행 분리, 검증 용이 | JSON 파싱 오류 처리 필요 | 최상 |
| Reflexion | 실패 시 반성·재시도 내장 | 자가 수정 강함 | 구현 복잡 | 최상 |

> **정확도 측면**: Query Plan JSON + 자가 수정 루프 = 이 과제 최적 조합.
> **권장**: Query Plan JSON (계획 수립) + ReAct (실행 단계 자가 수정).

---

## 단계 8. 답변 생성 & 환각 방지

### 8-A. 프롬프트 전략

| 후보 | 설명 | 장점 | 단점 | 정확도 |
|------|------|------|------|--------|
| **Chain-of-Thought (CoT)** | 단계적 추론 과정 명시 | 복잡한 질의 정확도 향상, think_trace 자동 생성 | 출력 길이 증가 | 상 |
| **Self-Consistency** | 같은 질의를 N번 추론 후 다수결 | 안정성 높음 | LLM 호출 N배 | 최상 |
| Zero-shot 단순 프롬프팅 | 직접 답변 요청 | 빠름 | 복잡한 질의에서 오류 多 | 하 |
| Few-shot 프롬프팅 | 예시 Q&A 포함 | 형식 일관성 | 예시 준비 비용 | 중상 |

> **정확도 측면**: Self-Consistency > CoT > Few-shot > Zero-shot
> **권장**: CoT 기본 적용. 중요 질의에 Self-Consistency (N=3) 추가.

### 8-B. 환각 방지 전략

| 전략 | 설명 | 효과 |
|------|------|------|
| **Fact Check 단계** | 생성된 답변을 검색 결과와 1:1 대조 | 최상 — 수치 오류 직접 검출 |
| **Grounded Generation** | 검색 결과만 참조하도록 프롬프트 강제 | 상 — 생성 단계에서 제어 |
| **Retrieved Context 제한** | 검색 결과 외 LLM 자체 지식 차단 | 상 |
| Citation 요구 | 모든 사실에 출처 번호 달기 | 중 — 출처 없는 내용 필터 가능 |

> **권장**: Grounded Generation (생성 시) + Fact Check (생성 후). 두 단계 중복 적용.

### 8-C. 답변 불가 처리

| 전략 | 설명 | 정확도 (감점 방지) |
|------|------|------------------|
| **규칙 기반 사전 감지** | 패턴 매칭으로 즉시 차단 | 최상 (빠르고 확실) |
| **LLM 판단** | "이 질의가 데이터로 답 가능한가?" | 상 (미묘한 케이스 처리) |
| **결과 없음 → 자동 거부** | DB 검색 결과 없으면 거부 | 상 (실행 후 감지) |
| **두 단계 결합** | 규칙 기반 → LLM 판단 순서 | 최상 |

---

## 단계 9. API 서버

### 9-A. 웹 프레임워크

| 후보 | 장점 | 단점 | 권장 |
|------|------|------|------|
| **FastAPI** | async 지원, 자동 문서화, 타입 힌트 | — | **권장** |
| Flask | 경량, 단순 | async 지원 약함, 수동 문서화 | 대안 |
| Django REST | 대규모 기능 | 과도한 복잡성 | 비권장 |

### 9-B. 배포 환경 (NCP)

| 후보 | 장점 | 단점 | 권장 |
|------|------|------|------|
| **NCP VM + Docker** | 가장 범용, Public IP 할당 용이, 설정 자유 | 서버 관리 직접 | **권장** |
| NCP NKS (Kubernetes) | 스케일링, 무중단 배포 | 설정 복잡, 오버스펙 | 대안 |
| NCP Cloud Functions | 서버리스, 비용 효율 | 콜드스타트 지연, 60초 제한 위험 | 비권장 |

### 9-C. 비동기 처리 전략

| 전략 | 설명 | 정확도 영향 |
|------|------|------------|
| **async/await** | FastAPI 기본 비동기, 여러 DB 병렬 호출 | 60초 내 더 많은 검증 단계 수행 가능 |
| 동기 처리 | 순차 실행 | 단순하지만 병렬 가능한 서브질의도 순차 실행 |

> **권장**: 의존성 없는 서브질의는 asyncio.gather()로 병렬 실행 → 전체 응답 시간 단축 → 남은 시간을 검증에 투자.

---

## 전체 기술 선택 요약표

| 단계 | 정확도 최우선 선택 | 대안 (빠른 개발) |
|------|-----------------|----------------|
| 정형 전처리 | pandas + 명시적 NULL | polars |
| PDF 파싱 | pymupdf + 섹션 단위 청크 | markitdown |
| 온톨로지 | Protégé 수동 설계 | LLM 초안 + 수동 검토 |
| RDB | PostgreSQL + DuckDB 병행 | SQLite |
| Vector DB | Qdrant + Hybrid Search | Chroma + Dense-only |
| 임베딩 | HyperCLOVA X + multilingual-e5 | HyperCLOVA X 단독 |
| Graph DB | Neo4j + Apache Jena | Neo4j 단독 |
| NL2SQL | Schema Linking + CoT + Self-correction | Few-shot Prompting |
| 벡터 검색 | Hybrid (BM25 + Dense) + LLM Re-ranking | Dense-only |
| Intent 분류 | 규칙 기반 + LLM Query Plan JSON | LLM 단독 분류 |
| Agent 패턴 | Query Plan JSON + ReAct + Self-correction | ReAct 단독 |
| Agent 프레임워크 | LangGraph | LangChain |
| 답변 생성 | CoT + Self-Consistency + Fact Check | CoT 단독 |
| 환각 방지 | Grounded Generation + Fact Check 2단계 | Grounded Generation 단독 |
| API 서버 | FastAPI + async | FastAPI + 동기 |
| 배포 | NCP VM + Docker | NCP VM 직접 |

---

## 정확도에 가장 큰 영향을 주는 선택 TOP 5

```
1위. Schema Linking 하드코딩
     수익률·위험등급·판매여부 컬럼이 테이블마다 달라 LLM이 혼동.
     하드코딩으로 정확히 매핑하면 NL2SQL 정확도 대폭 향상.

2위. Hybrid Search (BM25 + Dense) + LLM Re-ranking
     Dense-only 대비 Recall 20~30% 향상.
     "에코프로비엠"(키워드) + "2차전지 소재"(의미) 동시 대응.

3위. Query Plan JSON + 자가 수정 루프
     실행 전 계획 수립으로 의존성 오류 방지.
     SQL 오류·빈 결과 시 자동 수정으로 답변 누락 최소화.

4위. Fact Check 단계
     환각으로 인한 감점이 평가에서 치명적.
     생성 답변과 검색 결과 1:1 대조로 수치 오류 차단.

5위. ETF 구성종목 외부 데이터 (RDB 적재)
     이 데이터 없이는 난이도 [중] 이상 편입종목 질의 처리 불가.
     Vector 검색보다 RDB JOIN이 정확도 높고 비중 정렬도 가능.
```
