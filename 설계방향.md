# 금융상품 Agent — 시스템 설계 방향

> 설계 원칙: **정확도 최우선** / 응답 시간 60초 이내

---

## 1. 전체 아키텍처

```
사용자 질의 (자연어)
        │
        ▼
[Step 1] 답변 불가 조기 감지           (규칙 기반, LLM 호출 없음)
        │
        ▼
[Step 2] 질의 분석 + Query Plan 생성   (LLM 호출 1~2회)
        │
        ▼
[Step 3] Schema Linking                (하드코딩 + LLM 재확인)
        │
        ▼
[Step 4] 서브질의 실행 + 자가 수정 루프 (DB 호출 + LLM 호출 N회)
         ├── RDB  (NL2SQL)
         ├── Vector DB (의미 검색)
         └── Graph DB  (관계 탐색)
        │
        ▼
[Step 5] 결과 통합 + 재검증            (LLM 호출 1회)
        │
        ▼
[Step 6] 답변 생성                     (LLM 호출 1회)
        │
        ▼
[Step 7] Fact Check                    (LLM 호출 1회)
        │
        ▼
API 응답 반환 (retrieved_context / think_trace / answer)
```

**LLM 호출 예산**: 최대 10회 / 회당 3~5초 가정 → 60초 이내 충분

---

## 2. KnowledgeBase 설계 — 세 DB의 역할

### 핵심 개념: 같은 데이터를 목적에 맞게 다르게 표현

```
원본 xlsx (4개 테이블, 14.5만 건)
        │
        ├──→ RDB       : 원본 구조 그대로 (조건 필터·정렬·집계)
        ├──→ Vector DB : 자연어 문장으로 변환 후 임베딩 (의미 검색)
        └──→ Graph DB  : 관계(엔티티↔엔티티)만 추출 (관계 탐색)
```

---

### 2-A. RDB

**역할**: NL2SQL로 생성된 SQL 실행. 조건 필터링·정렬·집계 전담.

4개 테이블 전체를 그대로 적재한다. 질의의 대부분은 여기서 해결된다.

| 테이블 | 주요 필터 컬럼 | 주요 정렬 컬럼 |
|--------|--------------|--------------|
| bond_kr | CRD_GRD, PD_RISK_GCD, CURR_CD, BD_KND, MAT_DT | BUY_YIELD, DUR, EVAL_PRICE |
| etf_kr | wu_inv_ast_type, wu_inv_rgn, pd_risk_nm, pd_pen_tr_yn | cu_charge_rt, du_last_aum, du_er_1y |
| etf_gl | pd_exg_mkt_cd, wu_inv_ast_type, wu_inv_rgn, pd_trd_ccy | cu_charge_rt, du_last_aum |
| fund | or_attr_desc, fd_ivst_rgn_desc, zrin_fd_ivst_risk_grd_nm, sale_yn | fd_yr1_ern_r, fd_nast_suma |

**인덱스 전략**: 위험등급, 자산군, 투자지역, 수익률, 만기일 컬럼에 인덱스 생성.

---

### 2-B. Vector DB

**역할**: 자연어·테마·의미로 상품을 찾을 때. RDB 컬럼으로 표현 불가능한 질의 처리.

**어떤 질의를 담당하는가**:
- "AI 반도체 관련 ETF" → "AI 반도체"는 컬럼값이 아닌 의미
- "캠브리콘이 편입된 중국 ETF" → 운용전략 텍스트에서 탐색
- "배당 성장 전략 ETF" → cu_strtegy 의미 검색

---

#### Input Data 설계 원칙

**원본 정형 데이터를 그대로 넣으면 안 된다.**
컬럼명과 코드값은 임베딩 모델이 의미를 이해하지 못한다.
Vector DB에 넣는 것은 정형 데이터를 **의미 있는 자연어 텍스트로 변환한 결과물**이다.

**임베딩 텍스트 ≠ 수치·구조 정보**

수치 데이터(총보수, AUM, 수익률)는 텍스트에 포함하지 않는다.
→ 수치는 RDB가 담당하고, 수치를 텍스트에 포함하면 임베딩 벡터가 희석된다.
→ 수치는 **메타데이터 페이로드**로 별도 저장하여 필터에만 사용한다.

```
[잘못된 예]
"TIGER 미국S&P500, 총보수 0.07%, AUM 3조원, 1년수익률 18.5%,
 fp:DomesticETF a owl:Class ; rdfs:subClassOf fp:ETF ..."
→ 임베딩 벡터가 수치·TTL 문법 노이즈로 희석됨

[올바른 예]
"TIGER 미국S&P500은 삼성자산운용이 운용하는 미국 주식형 ETF입니다.
 S&P 500 지수를 추종하며, 연금거래가 가능합니다."
→ 의미·테마 검색에 집중된 임베딩
```

---

#### 청크 분리 전략 (목적별로 분리)

하나의 상품 정보를 **검색 목적에 따라 별도 청크**로 나눠 각각 색인한다.
임베딩 모델은 텍스트 전체를 벡터 하나로 압축하므로, 내용이 섞이면 어느 질의에도 완벽히 매칭되지 않는다.

```
상품: QQQ (해외ETF)
    │
    ├── [청크 1] 상품 기본 요약 (모든 상품에 생성)
    │   "QQQ는 Invesco가 운용하는 미국 주식형 ETF.
    │    NASDAQ-100 지수 추종, 미국 증시 상장"
    │   metadata: {id, table, region, asset_type, expense_ratio, aum, ...}
    │
    └── [청크 2] 운용전략 원문 (해외ETF — cu_strtegy가 있는 경우만)
        "The fund tracks the Nasdaq-100 Index. Key holdings include
         Apple, NVIDIA, Microsoft, AMD, Broadcom (semiconductors)..."
        metadata: {id, table, chunk_type: "strategy"}
```

**청크 수 요약**:

| 테이블 | 청크 수 | 이유 |
|--------|---------|------|
| 국내채권 (42,394건) | 건당 1개 | 운용전략 텍스트 없음 |
| 국내ETF (1,734건) | 건당 1~2개 | cu_strtegy 있는 경우 추가 |
| 해외ETF (5,646건) | 건당 2개 | cu_strtegy 필수 별도 청크 |
| 공모펀드 (95,619건) | 건당 1개 | 텍스트 컬럼 제한적 |

---

#### 임베딩 텍스트 생성 방법: 템플릿 기반 자동 생성

14.5만 건을 LLM으로 일괄 요약하면 비용이 과다하다.
**고정 템플릿**으로 자동 생성하고, LLM은 운용전략 등 소수 케이스에만 활용한다.

```python
# 국내ETF 템플릿
def etf_kr_to_text(row):
    pension = "연금거래 가능합니다." if row['pd_pen_tr_yn'] == 'Y' else ""
    return f"""{row['pd_nm']}은 {row['cu_fund_mgmt_co']}이 운용하는 \
{row['wu_inv_ast_type']} ETF입니다. \
기초지수는 {row['cu_base_index'] or '미공개'}를 추종하며, \
투자지역은 {row['wu_inv_rgn']}입니다. \
위험등급은 {row['pd_risk_nm']}입니다. {pension}""".strip()

# 해외ETF 청크1 (요약)
def etf_gl_summary_to_text(row):
    return f"""{row['pd_nm']} ({row['pd_abrv_nm']})은 \
{row['cu_fund_mgmt_co']}이 운용하는 {row['wu_inv_ast_type']} ETF입니다. \
{row['pd_exg_mkt_cd']} 거래소에 {row['pd_trd_ccy']}로 상장됩니다. \
기초지수: {row['cu_base_index']}, 투자지역: {row['wu_inv_rgn']}"""

# 해외ETF 청크2 (운용전략 — 그대로 사용)
def etf_gl_strategy_to_text(row):
    return row['cu_strtegy']  # 이미 자연어, 변환 불필요

# 국내채권 템플릿
def bond_kr_to_text(row):
    buyable = f"매수가능." if row['BUYABLE_QUANTITY'] else "매수불가."
    return f"""{row['PD_NM']}은 {row['BD_KND']}입니다. \
신용등급 {row['CRD_GRD']}, 위험등급 {row['PD_RISK_GCD']}등급. \
통화: {row['CURR_CD']}, 만기: {row['MAT_DT']}. {buyable}"""

# 공모펀드 템플릿
def fund_to_text(row):
    return f"""{row['itm_nm']}은 {row['or_attr_desc']} 펀드입니다. \
투자지역: {row['fd_ivst_rgn_desc']}, 벤치마크: {row['bmrk_nm']}. \
환헤지: {row['exchdg_yn']}, 위험등급: {row['zrin_fd_ivst_risk_grd_nm']}. \
판매여부: {row['sale_yn']}"""
```

**결측값 처리**: 일부 컬럼이 null인 경우 텍스트에서 제외하거나 "미공개"로 표기.

---

#### TTL과 Vector DB의 관계

TTL 파일의 역할과 Vector DB의 역할은 다르다. 혼동하지 않는다.

| 항목 | Graph DB | Vector DB |
|------|----------|-----------|
| TTL 스키마 (`a owl:Class`, `owl:DatatypeProperty`) | **적재 대상** | **넣지 않음** (문법이 노이즈) |
| TTL의 자연어 설명 (`rdfs:label`, `rdfs:comment`) | 적재 | **선택적으로 추출해서 넣을 수 있음** |
| 용도 | 온톨로지 추론·관계 탐색 | 용어 개념 질의 대응 (선택) |

TTL에서 추출한 자연어 설명을 Vector DB에 별도 색인하면 "총보수가 뭐야?", "해외ETF랑 국내ETF 차이?" 같은 개념 질의에 대응할 수 있다.

```python
# TTL에서 자연어 설명만 추출해 Vector DB 색인 (선택)
onto_docs = [
    {
        "text": "총보수(expenseRatio)는 ETF·펀드 운용에 드는 연간 총 비용 비율. 낮을수록 유리.",
        "metadata": {"type": "ontology_term", "term": "expenseRatio"}
    },
    {
        "text": "해외ETF(ForeignETF)는 미국·유럽 등 해외 증시에 상장된 ETF. 거래통화가 외화.",
        "metadata": {"type": "ontology_term", "term": "ForeignETF"}
    }
]
```

---

#### 메타데이터 페이로드 (필터링용)

수치·코드값은 임베딩 텍스트에 포함하지 않고, 메타데이터로만 저장한다.
벡터 유사도 검색과 조건 필터를 동시에 수행할 수 있다.

```python
document = {
    "text": "TIGER 미국S&P500은 삼성자산운용이 운용하는 미국 주식형 ETF...",
    "metadata": {
        "id":            "A0000D0",
        "table":         "etf_kr",
        "asset_type":    "주식형",
        "region":        "미국",
        "expense_ratio": 0.07,      # 수치 필터용
        "aum":           3000000000000,
        "risk_grade":    4,
        "return_1y":     18.5,
        "pension_yn":    "Y",
        "chunk_type":    "summary"
    }
}

# 사용 예: "미국 주식형 ETF 중 AI 반도체 관련"
results = vectordb.search(
    query="AI 반도체 기술 성장",
    filter={"table": "etf_kr", "asset_type": "주식형", "region": "미국"},
    top_k=10
)
```

---

### 2-C. Graph DB

**역할**: 상품 간 관계, 상품↔운용사↔자산군↔지역 연결 관계 탐색.

**노드(Node)**:

| 노드 타입 | 출처 컬럼 |
|----------|----------|
| Product (상품) | 4개 테이블 전체 |
| Manager (운용사) | cu_fund_mgmt_co |
| Index (기초지수) | cu_base_index, bmrk_nm |
| AssetClass (자산군) | wu_inv_ast_type, or_attr_desc |
| Region (투자지역) | wu_inv_rgn, fd_ivst_rgn_desc |
| Market (상장시장) | pd_exg_mkt_cd |
| RiskGrade (위험등급) | pd_risk_nm, zrin_fd_ivst_risk_grd_nm |
| CreditGrade (신용등급) | CRD_GRD |

**엣지(Edge)**:

| 관계 | 방향 | 출처 |
|------|------|------|
| MANAGED_BY | Product → Manager | cu_fund_mgmt_co |
| TRACKS | ETF → Index | cu_base_index |
| BENCHMARKED_BY | Fund → Index | bmrk_nm |
| BELONGS_TO | Product → AssetClass | wu_inv_ast_type |
| INVESTS_IN | Product → Region | wu_inv_rgn |
| LISTED_ON | ETF → Market | pd_exg_mkt_cd |
| HAS_RISK | Product → RiskGrade | pd_risk_nm |
| HAS_CREDIT | Bond → CreditGrade | CRD_GRD |

**어떤 질의를 담당하는가**:
- "S&P 500 추종 ETF 전체" → Index(S&P500) → TRACKS 역방향
- "BlackRock 운용 미국 주식형 ETF" → Manager → MANAGED_BY 역방향 + 필터
- "에코프로 편입 ETF" → Vector로 후보 찾기 → Graph로 자산군 교차 검증

---

## 3. DB 라우팅 설계

### 핵심: 폴백(Fallback)이 아닌 Intent 기반 처음부터 직행

```
질의 입력
    │
    ├── 조건 검색 · 비교 · 집계   → RDB (NL2SQL)
    │   "AA- 이상 채권", "총보수 낮은 ETF 상위 5개"
    │
    ├── 의미 · 테마 검색          → Vector DB
    │   "AI 반도체 ETF", "배당 성장 전략"
    │
    ├── 관계 탐색                 → Graph DB → (필요시) RDB
    │   "S&P 500 추종 ETF 목록"
    │
    ├── 복합 질의                 → Vector + Graph + RDB 파이프라인
    │   "에코프로 자회사 편입 ETF 중 AUM 큰 것"
    │
    └── 데이터 없음               → 즉시 "확인할 수 없음"
        "AAAA 신용등급 채권"
```

### Federated Query 처리 (복합 질의)

각 DB가 자기 역할만 하고 결과를 다음 단계로 넘기는 파이프라인:

```
예: "에코프로 자회사 편입 ETF 중 순자산 큰 상품"

[Vector DB]  "에코프로 자회사" 운용전략 텍스트 의미 검색
                 → 후보 ID: [A, B, C]
                                 │
                    [Graph DB]   국내ETF 노드 교차 검증 (선택)
                                 → 확인된 ID: [A, B]
                                                  │
                                      [RDB]  WHERE id IN (A, B)
                                             ORDER BY du_last_aum DESC
                                             → 최종 결과
```

**결과 통합 방법**:

```python
# 교집합 (AND 조건 — 모든 조건 충족)
final_ids = rdb_ids & vector_ids & graph_ids

# 공통 식별자로 JOIN
# → 모든 DB에 pd_itm_no를 공통 키로 저장
```

---

## 4. Intent 분류 설계

### 방식: 2단계 분리 (단순 질의) + ReAct (복합 질의)

```
질의 입력
    │
    ▼
[규칙 기반 사전 필터]  ← LLM 호출 없음, 즉시 처리
    답변 불가 패턴 감지
    │
    ├── 답변 불가 → "확인할 수 없음" 즉시 반환
    │
    ▼
[HyperCLOVA X — Intent 분류 + 엔티티 추출]
    │
    ├── 단순 조건 질의 → sql_tool 직행 (2단계 분리)
    │
    └── 복합 질의      → ReAct Agent 진입
```

### Intent 분류 프롬프트 출력 형식

```json
{
  "answerable": true,
  "query_type": "complex",
  "target_tables": ["etf_kr"],
  "entities": {
    "asset_class": "주식형",
    "region": "미국",
    "sort": "총보수 ASC",
    "limit": 3
  },
  "sub_queries": [
    {
      "id": 1,
      "db": "vector",
      "purpose": "에코프로 자회사 편입 ETF 후보 탐색",
      "depends_on": [],
      "output": "candidate_ids"
    },
    {
      "id": 2,
      "db": "rdb",
      "purpose": "후보 중 순자산 기준 정렬",
      "depends_on": [1],
      "output": "ranked_products"
    }
  ]
}
```

---

## 5. 정확도 최우선 설계 상세

### Step 1. 답변 불가 조기 감지

```python
# 1차: 규칙 기반 (LLM 호출 없음)
UNANSWERABLE_PATTERNS = [
    r"AAAA|AAAAA",               # 존재하지 않는 신용등급
    r"내일|다음주|전망|예측",     # 미래 예측
    r"추천해줘|사야해|팔아야해",  # 단정적 투자 추천
    r"실시간|지금 현재가",        # 스냅샷 데이터로 불가
]

# 2차: LLM 기반 판단 (규칙으로 안 잡힌 경우)
# "이 질의가 제공 데이터(채권/ETF/펀드 마스터 스냅샷)로 답 가능한가?"
```

### Step 2. Schema Linking (하드코딩)

같은 개념이 테이블마다 컬럼명이 다른 것이 핵심 함정이다.

```python
SCHEMA_MAP = {
    "총보수":     [("etf_kr", "cu_charge_rt"),  ("etf_gl", "cu_charge_rt")],
    "순자산":     [("etf_kr", "du_last_aum"),   ("etf_gl", "du_last_aum"),
                  ("fund",   "fd_nast_suma")],

    # 수익률은 테이블마다 컬럼명이 다름
    "수익률 1년": [("etf_kr", "du_er_1y"),      ("fund", "fd_yr1_ern_r")],
    "수익률 6개월":[("etf_kr", "du_er_6m"),     ("fund", "fd_mm6_ern_r")],
    "수익률 1개월":[("etf_kr", "du_er_1m"),     ("fund", "fd_mm1_ern_r")],

    "위험등급":   [("etf_kr", "pd_risk_nm"),
                  ("bond_kr","PD_RISK_GCD"),
                  ("fund",   "zrin_fd_ivst_risk_grd_nm")],

    "신용등급":   [("bond_kr","CRD_GRD"), ("bond_kr","PD_EVCO_CRD_GRD")],
    "투자지역":   [("etf_kr", "wu_inv_rgn"), ("etf_gl","wu_inv_rgn"),
                  ("fund",   "fd_ivst_rgn_desc")],
    "만기일":     [("bond_kr","MAT_DT")],
    "운용사":     [("etf_kr", "cu_fund_mgmt_co"), ("etf_gl","cu_fund_mgmt_co")],
    "기초지수":   [("etf_kr", "cu_base_index"),   ("etf_gl","cu_base_index")],
    "판매가능":   [("bond_kr","BUYABLE_QUANTITY > 0"),
                  ("fund",   "sale_yn = '판매중'"),
                  ("etf_kr", "pd_sale_yn = '1'")],
    "연금거래":   [("etf_kr", "pd_pen_tr_yn = 'Y'")],
    "환헤지":     [("fund",   "exchdg_yn")],
}
```

### Step 3. NL2SQL 자가 수정 루프

```python
MAX_RETRY = 3

for attempt in range(MAX_RETRY):
    sql = llm_generate_sql(query, schema, few_shots)

    try:
        result = db.execute(sql)
    except SQLError as e:
        sql = llm_fix_sql(sql, error=str(e))   # 오류 피드백 후 재생성
        continue

    if len(result) == 0:
        sql = llm_relax_condition(sql, attempt) # 조건 완화 후 재시도
        result = db.execute(sql)

    if len(result) > 0:
        break

if len(result) == 0:
    return "해당 조건을 만족하는 상품이 없습니다."
```

### Step 4. SQL 결과 LLM 재확인

```
"아래 SQL과 실행 결과가 원래 질의의 요구사항을 충족하는가?
 누락된 조건이 있다면 SQL을 수정하라."
```

### Step 5. 결과 통합 재검증

```
"수집한 결과로 아래 체크리스트를 확인하라:
 1. 모든 필터 조건이 충족되었는가?
 2. 정렬 기준이 올바른가?
 3. 아직 탐색하지 못한 조건이 있는가?
 미충족 항목이 있으면 추가 검색을 수행하라."
```

### Step 6. Fact Check (답변 검증)

```
"생성된 답변의 각 수치·사실이 검색 결과에 존재하는지 1:1 대조하라.
 존재하지 않는 내용은 삭제하거나 '확인할 수 없음'으로 교체하라."
```

### LLM 호출 예산 정리

| 단계 | LLM 호출 | 목적 |
|------|---------|------|
| 답변 불가 판단 (2차) | 0~1회 | 감점 방지 |
| Query Plan 생성 | 1회 | 질의 분석 오류 방지 |
| Query Plan 자기 검토 | 1회 | 누락 조건 방지 |
| Schema Linking 재확인 | 1회 | 컬럼 매핑 오류 방지 |
| NL2SQL 생성 + 수정 | 1~3회 | SQL 오류 방지 |
| SQL 결과 재확인 | 1회 | 잘못된 결과 통과 방지 |
| 결과 통합 재검증 | 1회 | 조건 누락 방지 |
| 답변 생성 | 1회 | — |
| Fact Check | 1회 | 환각 방지 |
| **합계** | **최대 11회** | |

---

## 6. 온톨로지 설계

### 제출 파일 (필수)

```
ontology/
├── common.ttl      # fp:Product 공통 상위 클래스
├── bond_kr.ttl     # 국내채권
├── etf_kr.ttl      # 국내ETF
├── etf_gl.ttl      # 해외ETF
└── fund_pub.ttl    # 공모펀드
```

### 클래스 계층

```
fp:Product
├── fp:Bond
│   └── fp:DomesticBond
├── fp:ETF
│   ├── fp:DomesticETF
│   └── fp:ForeignETF
└── fp:Fund
    └── fp:PublicFund
```

### 온톨로지의 세 가지 역할

1. **평가 제출 요건 충족** (소스코드 20점 항목 — Ontology.ttl 미제출 시 감점)
2. **Graph DB 적재 대상**: 온톨로지 인스턴스 데이터를 RDF 트리플로 Graph DB에 로드
3. **LLM 가이드라인**: 각 상품군의 속성·관계를 LLM 프롬프트 맥락으로 제공
   → NL2SQL 정확도 향상, Schema Linking 보조

### TTL 활용 흐름

```
TTL 파일
    │
    ├── Graph DB 적재 (Apache Jena / Neo4j)
    │   스키마 정의 + 인스턴스 데이터 → RDF 트리플로 저장
    │
    ├── LLM 프롬프트 맥락 주입
    │   "fp:ForeignETF의 속성: expenseRatio, baseIndex, region..."
    │   → NL2SQL 생성 시 스키마 이해 보조
    │
    └── Vector DB (선택적, 극히 일부)
        rdfs:label + rdfs:comment 텍스트만 추출
        → 용어 개념 질의 대응용 별도 색인
        (TTL 문법 자체는 절대 넣지 않음)
```

---

## 7. API 서버 설계

### 엔드포인트

```
GET /answer
파라미터: question_id (string), question (string)
응답:     200 OK + JSON (확인 불가 질의도 동일 스키마)
제한:     60초 이내 응답
운영:     09.07 ~ 09.20 상시 활성
```

### 응답 스키마

```json
{
  "question_id": "Q-001",
  "question": "평가 질의 원문",
  "retrieved_context": "참조: 해외ETF마스터 / 기준일: 2026-07-11",
  "think_trace": "조건 파싱 → Vector 검색(에코프로 자회사) → SQL 실행(AUM 정렬) → Fact Check",
  "answer": "조건에 부합하는 상품: A(AUM 2조), B(AUM 8천억). 근거: 국내ETF마스터(2026-07-11)"
}
```

### 기술 스택

| 레이어 | 선택 |
|--------|------|
| API 서버 | FastAPI (async 지원) |
| RDB | PostgreSQL |
| Vector DB | Chroma 또는 FAISS |
| Graph DB | Neo4j Community |
| 임베딩 | HyperCLOVA X Embedding (NCP) |
| LLM | HyperCLOVA X (필수) |
| Agent 프레임워크 | LangGraph |
| 배포 | NCP VM + Docker |

---

## 8. 난이도별 처리 전략

### 하 — 단순 조건 조회

```
"현재 판매 가능한 원화채권 중 AA- 이상 종목"

→ Intent: 조건 검색 (단순)
→ RDB 직행
→ SQL: SELECT * FROM bond_kr
        WHERE CRD_GRD >= 'AA-'
        AND CURR_CD = 'KRW'
        AND BUYABLE_QUANTITY > 0
```

### 중 — 외부 지식 + 복합 조건

```
"캠브리콘이 편입된 중국 반도체 ETF"

→ Intent: 의미 검색 (편입 종목 정보는 컬럼에 없음)
→ Vector DB: cu_strtegy 텍스트에서 "Cambricon / 중국 반도체" 의미 검색
→ Graph DB: INVESTS_IN(중국) 교차 검증
→ RDB: 최종 상품 정보 조회
```

### 상 — 멀티-홉 + 추론

```
"에코프로의 자회사를 편입한 ETF 중 순자산이 큰 상품의 위험요인 알려줘"

→ Step 1: Vector("에코프로 자회사 편입") → ETF 후보
→ Step 2: RDB(AUM DESC) → 상위 N개
→ Step 3: Vector("위험요인") + 외부 문서 → 위험 설명
→ Fact Check → 답변 생성
```

### 답변 불가

```
"신용등급 AAAA인 채권 찾아줘"
"KODEX AI로봇 ETF 정보 알려줘"  ← 제공 데이터에 없는 종목

→ 규칙 기반 or LLM 판단: 데이터 없음
→ "확인할 수 없음: 제공 데이터(2026-07-11 기준)에 해당 조건의 상품이 없습니다."
→ 추가 답변 생성 금지 (감점)
```

---

## 9. 추가 고려사항 (검토 결과)

---

### 9-A. 결측값 처리 전략 (데이터 품질 이슈)

원본 데이터에는 구조적 결측이 존재한다. 이를 처리하지 않으면 SQL 오류, 잘못된 답변이 발생한다.

| 테이블 | 결측 항목 | 처리 방법 |
|--------|----------|----------|
| 국내ETF | 기초지수·총보수가 **일부 종목만 수록** | 해당 컬럼 NULL 처리 + 답변 시 "정보 없음" 명시 |
| 공모펀드 | **보수 정보 전체 미포함** | Schema Map에서 fund 보수 컬럼 제외, 질의 시 "보수 정보 미제공" 반환 |
| 국내채권 | 매수·세후 수익률은 **매수가능 종목 일부만** | BUYABLE_QUANTITY > 0 조건과 함께 처리 |

```python
# 보수 질의 시 공모펀드 자동 제외 예시
if "보수" in query and "펀드" in target_tables:
    note = "공모펀드는 보수 정보가 제공되지 않습니다."
    target_tables.remove("fund")
```

---

### 9-B. 위험등급·신용등급 비교 연산

두 등급 컬럼은 **문자열**이므로 ORDER BY나 범위 필터가 직접 되지 않는다.
사전에 숫자 매핑 컬럼을 추가하거나, SQL에서 CASE WHEN으로 처리해야 한다.

```python
# 전처리: 신용등급 숫자 매핑 컬럼 추가
CREDIT_GRADE_ORDER = {
    "AAA": 1, "AA+": 2, "AA": 3, "AA-": 4,
    "A+": 5,  "A": 6,  "A-": 7,
    "BBB+": 8,"BBB": 9,"BBB-": 10,
}
df["CRD_GRD_NUM"] = df["CRD_GRD"].map(CREDIT_GRADE_ORDER)

# 위험등급도 테이블마다 표기 방식이 다름
# bond_kr: 숫자 코드 (1, 2, 3...)
# etf_kr:  "높은위험(4등급)"
# fund:    "매우 낮은 위험"
# → 전처리 시 모두 숫자로 통일
```

```sql
-- SQL 예시: AA- 이상 채권
SELECT * FROM bond_kr WHERE CRD_GRD_NUM <= 4
```

---

### 9-C. 교차 상품군 질의 — UNION ALL 전략

"채권이랑 ETF 중 위험등급 1인 것"처럼 테이블을 넘나드는 질의는
컬럼명이 다르므로 **공통 alias로 UNION ALL** 처리한다.

```sql
SELECT 'bond'  AS product_type, PD_NM AS name, PD_RISK_GCD  AS risk_grade_num
FROM   bond_kr WHERE PD_RISK_GCD = 1
UNION ALL
SELECT 'etf_kr' AS product_type, pd_nm AS name, pd_risk_grade_num AS risk_grade_num
FROM   etf_kr   WHERE pd_risk_grade_num = 1
UNION ALL
SELECT 'fund'  AS product_type, itm_nm AS name, zrin_risk_grade_num AS risk_grade_num
FROM   fund     WHERE zrin_risk_grade_num = 1
ORDER BY product_type, name;
```

→ Schema Map에 교차 테이블 쿼리 패턴을 미리 정의해두면 NL2SQL 정확도 상승.

---

### 9-D. Entity Resolution — 운용사명 표기 통일

같은 운용사가 테이블마다, 데이터마다 다르게 표기될 수 있다.
Graph DB 구축 및 Vector DB 색인 전에 **전처리로 통일**해야 한다.

```python
MANAGER_ALIAS = {
    "삼성자산운용": ["삼성운용", "Samsung Asset Management", "삼성AM"],
    "미래에셋자산운용": ["미래에셋운용", "Mirae Asset", "미래에셋AM"],
    "BlackRock": ["블랙록", "Blackrock", "iShares"],
    "Invesco": ["인베스코"],
    "Vanguard": ["뱅가드"],
}

def normalize_manager(name: str) -> str:
    for canonical, aliases in MANAGER_ALIAS.items():
        if name in aliases or name == canonical:
            return canonical
    return name
```

---

### 9-E. Vector DB 검색 임계값(Threshold) 설정

유사도 임계값을 설정하지 않으면 관련 없는 결과가 Federated Query로 흘러들어
RDB에서 엉뚱한 상품을 반환하는 연쇄 오류가 발생한다.

```python
# 임계값 미만 결과는 버림
results = vectordb.search(query, top_k=20)
results = [r for r in results if r["score"] >= THRESHOLD]

if len(results) == 0:
    # 임계값 낮춰 재시도 (1회)
    results = vectordb.search(query, top_k=10, threshold=THRESHOLD * 0.8)

if len(results) == 0:
    return "관련 상품을 찾을 수 없습니다."
```

| 청크 유형 | 권장 임계값 | 이유 |
|-----------|------------|------|
| 상품 요약 | 0.75 이상 | 명확한 의미 매칭 요구 |
| 운용전략 텍스트 | 0.65 이상 | 영문 자유서술, 표현 다양 |
| 온톨로지 용어 설명 | 0.80 이상 | 개념 정의이므로 정확 매칭 |

---

### 9-F. think_trace 필드 설계

`think_trace`는 평가 항목이다 (사고·추론·도구 사용 과정). 구조화된 형식으로 기록한다.

```python
think_trace = []

think_trace.append("1. 질의 분석: 조건(미국 증시, 주식형, 총보수↓, AUM↑) 추출")
think_trace.append("2. 라우팅: 조건 검색 → RDB(NL2SQL) 선택")
think_trace.append("3. Schema Linking: wu_inv_rgn='미국', cu_charge_rt ASC, du_last_aum DESC")
think_trace.append("4. SQL 실행: SELECT ... FROM etf_gl WHERE ... ORDER BY ... LIMIT 3")
think_trace.append("5. 결과 검증: 3건 반환, 조건 충족 확인")
think_trace.append("6. Fact Check: 수치 1:1 대조 완료")

response["think_trace"] = " → ".join(think_trace)
```

---

### 9-G. retrieved_context 필드 설계

답변 근거를 구체적으로 표시한다. 평가 담당자가 검색 품질을 직접 확인하는 항목이다.

```python
retrieved_context = {
    "tables": ["해외ETF마스터 (PREF02N001)"],
    "data_date": "2026-07-11",
    "query_used": "SELECT pd_nm, cu_charge_rt, du_last_aum FROM etf_gl WHERE ...",
    "result_count": 3,
    "vector_chunks": []   # Vector 검색 사용 시 청크 ID 기록
}

response["retrieved_context"] = json.dumps(retrieved_context, ensure_ascii=False)
```

---

### 9-H. 임베딩 사전 처리 (오프라인 배치)

14.5만 건을 API 요청 시마다 실시간 임베딩하면 응답 시간 초과가 발생한다.
**서버 구동 전에 모든 청크를 임베딩하여 Vector DB에 적재**해야 한다.

```
[오프라인 — 서버 구동 전]
  xlsx 파싱 → 템플릿 텍스트 생성 → 배치 임베딩 → Vector DB 적재

[온라인 — API 요청 시]
  질의 텍스트만 임베딩 (1회) → Vector DB 검색
```

배치 임베딩 시 API rate limit 주의:

```python
import time

chunks = generate_all_chunks()  # 전체 청크 생성
BATCH_SIZE = 100

for i in range(0, len(chunks), BATCH_SIZE):
    batch = chunks[i:i+BATCH_SIZE]
    embeddings = embedding_api.encode([c["text"] for c in batch])
    vectordb.upsert(batch, embeddings)
    time.sleep(0.5)  # rate limit 대응
```

---

### 9-I. 날짜 컬럼 타입 처리

국내채권의 발행일(`ISU_DT`)·만기일(`MAT_DT`)이 `double precision` 타입으로 YYYYMMDD 숫자로 저장되어 있다.
SQL 날짜 비교 시 형변환이 필요하다.

```sql
-- 잔존만기 계산 예시
SELECT PD_NM,
       MAT_DT,
       MAT_DT - 20260711 AS remaining_approx  -- 근사값
FROM   bond_kr
WHERE  MAT_DT > 20270101  -- 1년 이상 잔존
```

```python
# 전처리 시 datetime 컬럼 추가
df["ISU_DT_parsed"] = pd.to_datetime(df["ISU_DT"].astype(int).astype(str), format="%Y%m%d", errors="coerce")
df["MAT_DT_parsed"] = pd.to_datetime(df["MAT_DT"].astype(int).astype(str), format="%Y%m%d", errors="coerce")
df["REMAINING_YEARS"] = (df["MAT_DT_parsed"] - pd.Timestamp("2026-07-11")).dt.days / 365
```

---

## 10. 외부 데이터 설계

과제 명세상 외부 데이터 수집·활용이 허용되며, 단 평가 시 주최 측 제공 데이터가 기준이다.
외부 데이터는 **제공 데이터만으로 처리 불가능한 질의** (편입종목 기반, 테마 기반)를 커버하기 위해 수집한다.

---

### 10-A. 어떤 DB에 넣을 것인가

**판단 기준**:

```
정형 데이터 (구조화, 컬럼으로 표현 가능)  →  RDB + Graph DB
비정형 데이터 (자유 텍스트)               →  Vector DB
```

금융용어사전·증권 용어는 **넣지 않는다.**
HyperCLOVA X가 이미 알고 있는 범용 지식이므로 추가해도 효과 없음.

---

### 10-B. ETF 구성종목 데이터 → RDB + Graph DB

**왜 필요한가**: "에코프로 자회사를 편입한 ETF" 류의 질의는 제공 데이터 컬럼으로 불가능.
국내ETF는 `cu_strtegy` 텍스트가 없거나 빈약하여 Vector 검색도 불가능.

**수집 출처**:
- KRX 정보데이터시스템 (data.krx.co.kr) — ETF 구성종목 공개
- 각 운용사 홈페이지 (삼성자산운용, 미래에셋 등)

**RDB 테이블 스키마**:

```sql
CREATE TABLE etf_holdings (
    etf_id       TEXT,          -- pd_itm_no와 JOIN 키
    stock_code   TEXT,          -- 종목코드
    stock_name   TEXT,          -- 종목명
    weight       NUMERIC,       -- 편입 비중 (%)
    holdings_dt  TEXT           -- 기준일
);

-- 활용 예: "에코프로비엠을 편입한 ETF"
SELECT e.pd_nm, h.weight
FROM   etf_kr e
JOIN   etf_holdings h ON e.pd_itm_no = h.etf_id
WHERE  h.stock_name = '에코프로비엠'
ORDER BY h.weight DESC;
```

**Graph DB 엣지 추가**:

```
(TIGER 2차전지TOP10) -[HOLDS {weight: 12.1}]-> (에코프로비엠)
(에코프로비엠) -[SUBSIDIARY_OF]-> (에코프로)
```

→ "에코프로 **자회사**를 편입한 ETF" = 2-hop 관계 탐색 가능

```cypher
-- Neo4j: 에코프로 자회사를 편입한 ETF
MATCH (parent:Stock {name: '에코프로'})
      <-[:SUBSIDIARY_OF]-(child:Stock)
      <-[:HOLDS]-(etf:Product)
RETURN etf.name, child.name
```

---

### 10-C. 기업-테마 매핑 데이터 → RDB + Graph DB

**왜 필요한가**: "우주항공 테마 ETF", "반도체 관련 ETF" 등 테마 기반 질의 처리.

**수집 출처**:
- KRX 테마지수 구성종목
- WISE INDEX 테마 분류
- FnGuide 섹터·테마 분류

**RDB 테이블 스키마**:

```sql
CREATE TABLE theme_mapping (
    theme        TEXT,   -- "우주항공", "2차전지", "반도체"
    company_code TEXT,   -- 종목코드
    company_name TEXT    -- 종목명
);

-- 활용 예: "우주항공 테마 ETF"
SELECT DISTINCT e.pd_nm, e.du_last_aum
FROM   etf_kr e
JOIN   etf_holdings h  ON e.pd_itm_no = h.etf_id
JOIN   theme_mapping t ON h.stock_code = t.company_code
WHERE  t.theme = '우주항공'
ORDER BY e.du_last_aum DESC;
```

**Graph DB 엣지 추가**:

```
(한화에어로스페이스) -[IN_THEME]-> (우주항공)
(TIGER 우주항공ETF) -[HOLDS]-> (한화에어로스페이스)
```

→ 최종적으로 테마 → 기업 → ETF 3-hop 탐색 가능

---

### 10-D. 투자설명서 PDF → Vector DB

**왜 필요한가**: 위험요인·운용 전략 설명 질의 처리. 국내ETF에는 `cu_strtegy` 텍스트가 없는 경우가 많음.

**수집 출처**:
- 금융감독원 전자공시시스템 (dart.fss.or.kr)
- 각 운용사 홈페이지 투자설명서 PDF

**처리 방법**:

```python
# PDF → 텍스트 추출 → 청크화 → 임베딩
import fitz  # pymupdf

def parse_prospectus(pdf_path, etf_id):
    doc = fitz.open(pdf_path)
    full_text = " ".join(page.get_text() for page in doc)

    # 핵심 섹션만 추출 (운용전략, 위험요인)
    chunks = split_into_sections(full_text)

    return [
        {
            "text": chunk,
            "metadata": {
                "etf_id": etf_id,
                "chunk_type": "prospectus",
                "source": "투자설명서"
            }
        }
        for chunk in chunks
    ]
```

**적재 우선순위**: 전체 1,734개 국내ETF 중 AUM 상위 300개 우선 처리.

---

### 10-E. 기업 기본정보 → RDB + Graph DB

**왜 필요한가**: 종목코드로 기업명·업종·시가총액 조회. ETF 구성종목 JOIN에 필요.

```sql
CREATE TABLE company_info (
    stock_code   TEXT PRIMARY KEY,
    company_name TEXT,
    sector       TEXT,   -- "IT", "소재", "산업재"
    industry     TEXT,   -- "반도체", "2차전지소재"
    market_cap   NUMERIC -- 시가총액
);
```

---

### 10-F. 외부 데이터 DB 배치 요약

| 외부 데이터 | RDB | Graph DB | Vector DB | 담당 질의 유형 |
|------------|:---:|:--------:|:---------:|--------------|
| ETF 구성종목 | **O** | **O** | X | 편입 기업 기반 ETF 탐색 |
| 기업-테마 매핑 | **O** | **O** | X | 테마 기반 ETF 탐색 |
| 투자설명서 텍스트 | X | X | **O** | 운용전략·위험요인 설명 |
| 기업 기본정보 | **O** | **O** | X | 종목코드 ↔ 기업명 매핑 |
| 금융용어사전 | X | X | X | **불필요** (LLM이 이미 앎) |

---

### 10-G. 외부 데이터 적용 시 Federated Query 전체 흐름

```
질의: "에코프로 자회사를 편입한 ETF 중 순자산이 큰 상품의 위험요인"

[Graph DB]
  (에코프로) -[PARENT_OF]-> (에코프로비엠, 에코프로머티리얼즈)
  (에코프로비엠, 에코프로머티리얼즈) <-[HOLDS]- ETF 후보 ID 목록
       │
       ▼
[RDB]
  SELECT pd_nm, du_last_aum, pd_risk_nm
  FROM   etf_kr
  WHERE  pd_itm_no IN (후보 ID 목록)
  ORDER BY du_last_aum DESC LIMIT 3
       │
       ▼
[Vector DB]
  "2차전지 ETF 위험요인" → 투자설명서 청크 검색
  → "원자재 가격 변동, 전기차 수요 둔화 리스크..."
       │
       ▼
[Answer Generator]
  세 결과 통합 → Fact Check → 근거 기반 답변
```

---

## 11. 설계 핵심 결정 사항 요약

| 결정 사항 | 선택 | 이유 |
|-----------|------|------|
| 정확도 vs 속도 | **정확도 최우선** | 60초 여유 충분 |
| DB 라우팅 방식 | **Intent 기반 직행** (폴백 아님) | 각 DB는 역할이 다름 |
| Intent 분류 | **LLM (Query Plan JSON)** | 복잡한 분해 처리 가능 |
| Schema Linking | **하드코딩 우선** | 컬럼명 불일치가 핵심 함정 |
| NL2SQL 오류 처리 | **자가 수정 루프 (최대 3회)** | SQL 오류·빈 결과 방지 |
| 환각 방지 | **Fact Check 단계 별도 운영** | 감점 방지 최우선 |
| LLM | **HyperCLOVA X** | 필수 요건 |
| Federated Query | **Query Plan 명시 생성 후 순차 실행** | 의존성 관리 |
| 외부 데이터 적재 | **정형 → RDB+Graph, 비정형 → Vector** | 데이터 성격에 따라 분리 |
| 금융용어사전 | **수집 안 함** | LLM이 이미 보유한 지식 |
